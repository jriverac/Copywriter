<p id="notice"><%= notice %></p>

<h1><%= @client.name %></h1>
<hr/>
<h2>Proyectos</h2>

<table border="1">
    <tr>
        <th>Nombre</th>
        <th>Descripci&oacute;n</th>
        <th colspan="3">Acciones</th>

    </tr>
<% @projects.each do |project| %>
  <tr>
    <td><%= link_to project.name, client_project_path(@client, project) %></td>
    <td><%= project.description %>&nbsp;</td>
    <td><%= link_to 'Show', client_project_path(@client, project) %></td>
    <td><%= link_to 'Edit', edit_client_project_path(@client, project) %></td>
    <td><%= link_to 'Destroy', client_project_path(@client, project), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Edit', edit_client_path(@client) %> |
<%= link_to 'Back', clients_path %>
